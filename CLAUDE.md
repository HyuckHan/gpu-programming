# GPU 프로그래밍 (2026-2학기, 동덕여대) 실습 자료

## 하드웨어 — 실습실 PC 전량 동일
- RTX 4060 (Ada Lovelace, sm_89), 24 SM, 8GB VRAM
- 대역폭 272 GB/s, L2 24MB, FP32 15.1 TFLOPS
- SM당 최대 1536 스레드 → 1024 스레드 블록은 occupancy 66%
- PCIe 4.0 x8 (호스트-디바이스 전송 실측 12 GB/s 내외)

## 빌드
- `nvcc -arch=sm_89 -O3` 고정. 다중 아키텍처 빌드 불필요
- 학생이 nvcc 플래그를 직접 치지 않게 한다. Makefile로만 빌드

## 코딩 규약
- 자료형 float 고정. double 금지 (FP32의 1/64 속도)
- **변수명과 코드 구조는 extracted/ 의 슬라이드 코드를 그대로 따른다.**
  학생이 수업에서 본 이름과 달라지면 안 된다.
- 학생이 채울 곳만 `// TODO:` 로 비우고, 나머지는 완성 상태로 제공
- 주석은 한국어, 식별자는 영어

## 문제 크기
- 행렬곱 기본 N=4096, 상한 8192 (VRAM 8GB)
- L2(24MB) 대비 실험용으로 N=1024 조건도 함께 제공
  - N=1024 → 행렬 3개 12MB, L2에 들어감
  - N=4096 → 201MB, L2 초과
- 벡터 연산은 배열 3개 합계가 100MB를 넘도록

## 산출물 규칙
각 `labs/<주차>-<주제>/` 에:
- `README.md` — 목표 한 줄, 빌드·실행, 검증 방법, 제출물, 배점
- `Makefile`, `skeleton.cu`, `broken_*.cu`, `verify.py`

## 대상
컴퓨터공학 4학년 전공선택. C 포인터가 흐릿한 학생이 섞여 있다고 가정한다.
빈 화면에서 시작시키지 말고, 동작하는 코드의 일부를 채우게 한다.

## 공개 범위
- 이 저장소는 비공개다. 학생 배포는 LMS zip으로 별도 수행한다.
- broken_*.cu 는 학생에게 배포하는 실습 자료다. 정상 커밋한다.
  (타일드 커널 완성본은 이미 강의 슬라이드에 있으므로 감출 대상이 아니다)
- solutions/ 만 예외: 슬라이드에 답이 없는 D유형 과제
  (lab13 scan 변형, lab14 전치 행렬곱)의 정답은 이 저장소에 두지 않는다.

## common.cuh 범위
- common.cuh에는 랩 공통 인프라만 둔다:
  CUDA_CHECK 매크로, cudaEvent 타이머, 비교 헬퍼(float 허용오차 / byte 정확일치).
- CPU 참조 구현, 데이터 생성, 인자 파싱은 각 랩의 .cu 안에 둔다.
  랩마다 자료형과 문제 구조가 다르므로 common으로 올리지 않는다.
- 정수 연산 커널(blur, histogram)은 byte 정확일치로 비교한다.
  부동소수점 허용오차 로직을 쓰지 않는다.

## 스칼라 출력 랩 (reduction)
- 출력이 배열이 아니라 스칼라 하나인 랩이 있다. compare_scalar 를 쓴다.
- CPU 참조는 double 로 누적한다. float 순차 누적은 2^24 부근에서 값이
  묻혀 정상 GPU 코드가 FAIL 로 판정된다.
- CPU 참조 컴파일에 -ffast-math 를 쓰지 않는다. 컴파일러가 덧셈 순서를
  바꾸면 정밀도 시연이 성립하지 않는다.

## 성능 지표
- 연산 위주 커널(matmul)은 GFLOP/s, 메모리 위주 커널(reduction)은 GB/s.
- reduction 의 유효 대역폭은 (입력 바이트 수 / 시간) 으로 정의한다.
  중간 단계에서 발생하는 추가 트래픽은 세지 않는다. 낭비가 많은 버전일수록
  이 값이 낮게 나오는 것이 의도된 동작이다.

## lab04 배포
- lab04 배포 zip 에는 lab02 의 vecadd.cu 와 common.cuh 를 복사해 넣는다.
  저장소에는 중복 보관하지 않는다.
- divergence.cu 의 두 커널은 조건문 한 줄만 다르고 나머지는 동일해야 한다.
  공통부를 함수나 템플릿으로 묶지 않는다. 학생이 diff 로 확인하는 것이 목적이다.

## 성능 비교 기준선
- 성능 비교의 기준선(naive)은 항상 고정된 실행 구성으로 측정한다.
  파라미터를 스윕할 때 기준선까지 같이 바뀌면 배속을 해석할 수 없다.
- lab05/lab06/lab07 은 같은 행렬곱의 3단계다. 커널 구현이 바뀌면
  세 랩을 함께 확인한다.

## lab09 배포
- lab09 배포 zip 에는 lab03 의 pgm.h 와 common.cuh 를 복사해 넣는다.
  pgm.h 를 랩마다 새로 만들지 않는다.
- 컴파일 타임 상수를 스윕하는 랩은 sweep.sh 가 재빌드까지 수행한다.

## 슬라이드에 없는 부분
- 슬라이드 커널이 블록 내부만 처리하는 경우(scan 의 partialSums 등),
  다중 블록 조립은 슬라이드에 없으므로 호스트 코드에 완성 제공한다.
  학생이 채우는 것은 슬라이드에 근거가 있는 부분으로 한정한다.
