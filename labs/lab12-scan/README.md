# lab12 — 스캔 (Kogge-Stone)

**목표:** 경쟁 상태가 실제로 어떤 결과를 내는지 관찰하고, 스캔 커널의 최적화 단계를 측정한다.

`scan.cu` 하나에 커널 다섯 개가 들어 있다. 두 개는 완성 상태로 제공되고,
나머지 세 개를 차례로 채운다.

## 이 커널들은 블록 안에서만 스캔한다

슬라이드의 커널은 자기 블록 구간만 스캔하고, 그 구간의 합을
`partialSums[blockIdx.x]`에 남긴다. 전체 배열을 스캔하려면 세 단계가 필요하다.

1. 블록별 스캔 — 슬라이드의 커널
2. `partialSums` 스캔
3. 각 블록에 앞선 블록들의 합 더하기

**2단계와 3단계는 슬라이드에 없다.** 그래서 호스트 코드와 함께 완성 상태로
제공한다. 여러분이 채우는 것은 1단계 커널의 본체뿐이다.
2단계를 블록 하나로 끝내려고 `N`을 `BLOCK_DIM x BLOCK_DIM` 이하로 제한한다.

## 1. 먼저 그냥 돌려 본다

```
make
./scan
```

`race` 커널은 슬라이드 8의 코드 그대로다. `__syncthreads()`가 루프 끝에
분명히 있는데도 결과가 틀린다. **세 번 반복 실행해서 표를 채운다.**

```
for i in 1 2 3; do ./scan | grep race; done
```

| 회차 | 첫 불일치 idx | 기대 | 실제 |
|---|---|---|---|
| 1 | | | |
| 2 | | | |
| 3 | | | |

이 시점에 `global`, `shared`, `exclusive`는 `(미구현)`으로 나온다.
아직 안 채웠으니 정상이다. `double`은 완성 제공된 것이라 바로 `PASS`가 나온다.

## 2. global 채우기

`scan_global_kernel`의 루프 본문을 채운다. 슬라이드 10이다.
`race` 커널과 알고리즘은 같고 루프 본문만 다르다.
`float v`를 두어 **읽는 시점과 쓰는 시점을 갈라 놓는 것**이 요점이다.

```
make
./scan
```

| 버전 | 시간(ms) | 판정 |
|---|---|---|
| race | | |
| global | | |

## 3. shared 채우기

`scan_shared_kernel`의 루프 본문을 채운다. 슬라이드 13이다.
적재와 저장은 이미 되어 있다. 구조는 슬라이드 10과 같고,
`output[i]` 대신 `buffer_s[threadIdx.x]`를 쓴다.

```
make
./scan
make test
```

| 버전 | 시간(ms) | 판정 |
|---|---|---|
| global | | |
| shared | | |
| double | | |

**세 값의 순서를 그대로 적어라.** 예상과 다르게 나올 수 있다.
그렇다면 그것이 관찰 결과이고, 토론 문제 2번에서 다룬다.

## 4. exclusive 채우기 — 도전 과제

`scan_exclusive_kernel`의 적재 부분을 채운다. 슬라이드 17이다.
루프와 마무리는 `double` 커널과 완전히 같다. 다른 곳은 처음 적재뿐이다.
inclusive는 자기 값까지 더하고 exclusive는 자기 앞까지만 더한다.

**제출: 선택**

## 빌드와 실행

```
make            # scan 을 빌드한다
make run        # ./scan 1048576 과 같다
make test       # verify.py 로 자체 점검한다
make clean
make help
```

인자는 `./scan <N>` 형태이고, 생략하면 `1048576`(1<<20)이다.
`N`은 `BLOCK_DIM`(1024)의 배수여야 하고 `1048576` 이하여야 한다.
조건에 맞지 않으면 프로그램이 실행 전에 막는다.

시간은 커널마다 100회 반복한 평균이다. `N`이 작아 1회 측정은 흔들린다.
세 단계 전체를 재고, 어느 단계인지는 구분하지 않는다.

## 검증

```
make test
```

`verify.py`는 **shared 커널만** 판정한다. `race`는 틀리는 것이 정상이고,
`double`은 완성 제공된 것이며, `exclusive`는 선택 과제다.

CPU 참조는 `double`로 누적한다. `float`로 누적하면 뒤로 갈수록 참조 자신이
어긋나서 기준 구실을 못 한다. 리덕션 랩에서 확인한 것과 같은 이유다.
비교는 배열 전체의 대표 크기 대비 상대오차 1e-3까지 통과로 친다.

## 토론 문제

**1.** `race` 커널에는 `__syncthreads()`가 루프 끝에 분명히 있다.
그런데도 결과가 틀리고, 틀리는 위치가 실행할 때마다 달라진다.
루프 한 번 안에서 어떤 스레드가 무엇을 읽고 무엇을 쓰는지 적어 보라.
`output[i - stride]`를 읽는 스레드와 `output[i]`에 쓰는 스레드가 각각 누구인가?
그 둘 사이에 기다리는 지점이 있는가?

**2.** `shared`와 `double`은 무엇이 다른가?
**루프 한 번에 `__syncthreads()`를 몇 번 부르는지 세어 보라.**
그리고 3절 표의 세 값을 보라. 차이가 예상만큼 크지 않다면,
전체 시간에서 동기화가 차지하는 몫이 어느 정도라는 뜻인가?

**3.** 이 커널들은 블록 안에서만 스캔한다.
전체 배열을 스캔하려면 무엇이 더 필요한가?
`scan.cu`의 호스트 코드를 읽고, 세 단계가 각각 무엇을 하는지 적어 보라.
`N`을 `BLOCK_DIM x BLOCK_DIM` 이하로 제한한 이유는 무엇인가?

## 제출

- 1절 race 반복 실행 표
- 2절 race 대 global 표
- 3절 global 대 shared 대 double 표
- 토론 문제 1, 2, 3 답변

**제출: 필수** (4절 exclusive 는 선택)

전체 13개 실습 중 10개 이상 제출해야 P 를 받을 수 있다.

## 참고

실습실 PC는 RTX 4060(sm_89) 기준이다. Makefile이 꽂혀 있는 GPU를 조회해
아키텍처를 자동으로 맞추므로 다른 PC에서도 `make`만 치면 된다.
